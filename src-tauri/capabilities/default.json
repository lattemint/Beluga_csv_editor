{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the CSV editor window. The editor uses web APIs (File System Access, localStorage); Tauri APIs are only used for the close-requested flow.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:allow-listen", "core:window:allow-destroy"]
}
